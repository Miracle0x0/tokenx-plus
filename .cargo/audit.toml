[advisories]
# Bincode 1 defines the current on-disk cache format. Replacing it requires a
# versioned cache migration; the advisory is maintenance-only, not a vulnerability.
ignore = ["RUSTSEC-2025-0141"]
