tui:
  ui:
    dialog:
      client_picker:
        title: " Clients "
        filter_label: "Filter: "
        filter_placeholder: "Type to filter..."
        no_results: "  No results"
        hint_full: "↑↓ • Space • * invert matches • Enter • Esc"
        hint_medium: "* invert matches • Space • Enter • Esc"
        hint_short: "* invert matches"
        error_empty_selection: "Select at least one client"
      group_by:
        title: " Group By "
        current_label: "Current: "
        current_suffix: "  current"
        hint: "↑↓ navigate • Enter select • Esc close"
        option:
          model:
            label: "Model"
            description: "One row per model; merge clients and providers (default)"
          client_model:
            label: "Client + Model"
            description: "One row per client-model pair"
          client_provider_model:
            label: "Client + Provider + Model"
            description: "Keep provider identity; no model merging"
          workspace_model:
            label: "Workspace + Model"
            description: "Group local usage by workspace, then model"
      pricing_sources:
        title: " Pricing source order "
        hint: "Up/Down select | k/j move | Enter apply | Esc cancel"
        priority: "Priority (highest first): custom overrides are always applied before this list."
        deepseek: "DeepSeek V4: OpenRouter time-period pricing remains first when available."
        description: "Use Up/Down to select a catalog, k/j to move it, r to restore the default order."
